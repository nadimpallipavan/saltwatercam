# SaltWaterCam V1 React Starter

Responsive React + Vite starter for SaltWaterCam V1.

## Install
```bash
npm install
```

## Run locally
```bash
npm run dev
```

## Production build
```bash
npm run build
npm run preview
```

## Replace before launch
- Replace placeholder live stream in `src/components/LiveStream.jsx`
- Add real logo assets in `src/assets`
- Update SEO domain in `public/sitemap.xml` and `public/robots.txt`
- Deploy `dist/` to Vercel, Netlify, or any static host

## Version History
Detailed changes between versions are documented in [CHANGELOG.md](file:///Users/pavanraju/.gemini/antigravity/scratch/saltwatercam/CHANGELOG.md).
- **v1.0.0 (Production / V1)**: Split pages, mobile Safari logo fixes, real Palm Beach County ERM reef metrics, production cPanel deployment.
- **v1.1.0 (Staging / V2)**: Advanced FaceID 360° biometrics, cryptographic local fallback mode, hybrid biometric caching, staging cPanel deployment.
- **v1.2.0 (Pending)**: Upcoming client-requested features.

